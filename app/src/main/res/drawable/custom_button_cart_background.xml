<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="270"
        android:startColor="@color/shop_detail_cart_start_color"
        android:endColor="@color/shop_detail_cart_end_color"/>

    <!-- 圆角-->
    <corners android:radius="50dp" />
    <!--描边-->
    <stroke
        android:width="2dp"
        android:color="@color/shop_detail_cart_stroke_color" />

</shape>